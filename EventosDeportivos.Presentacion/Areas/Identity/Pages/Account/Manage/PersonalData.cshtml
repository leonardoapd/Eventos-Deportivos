@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Personal Data";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-12">
        <p>Su cuenta contiene datos personales que nos ha proporcionado. Esta página le permite eliminar esos datos.</p>
        <p>
            <strong>La eliminación de estos datos eliminará permanentemente su cuenta y no se podrá recuperar.</strong>
        </p>
        @* <form id="download-data" asp-page="DownloadPersonalData" method="post" class="form-group">
            <button class="btn btn-primary" type="submit">Descargar</button>
        </form> *@
        <p>
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-primary">Borrar</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}